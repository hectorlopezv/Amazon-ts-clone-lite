import * as ActionTypes from '../Actions/ActionTypes';

const initialState = {
    Hector: 'null',
}

export const AppReducer = (state = initialState, action: any) => {
    //if(action.type === actionTypes.x){return {...state, algonuevo}}
    return state;
}


